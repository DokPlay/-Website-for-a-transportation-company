# Инструкция по деплою на Cloudflare Pages

## ✅ Что уже готово

Проект полностью подготовлен и протестирован локально:

- ✅ Структура проекта создана в `/home/user/webapp/`
- ✅ Git репозиторий инициализирован
- ✅ Все зависимости установлены
- ✅ Проект успешно собран (`npm run build`)
- ✅ Локальный сервер работает на порту 3000
- ✅ Статические файлы доступны по `/static/*`
- ✅ API endpoints работают (`/api/health`)
- ✅ README.md создан с полной документацией
- ✅ cloudflare_project_name сохранён как `logistikpro`

## 🌐 Тестовый URL

**Локальная версия доступна по адресу:**
https://3000-ixer21c7xgdalr3gap0ul-dfc00ec5.sandbox.novita.ai

Вы можете протестировать:
- Главную страницу: `/`
- Калькулятор: `/static/calculator.html`
- Форму заявки: `/static/request.html`
- Контакты: `/static/contacts.html`
- API: `/api/health`

## 🚀 Деплой на Cloudflare Pages

### Шаг 1: Настройка Cloudflare API ключа

**ВАЖНО:** Перед деплоем необходимо настроить API ключ Cloudflare.

1. Перейдите во вкладку **Deploy** в боковой панели
2. Следуйте инструкциям для создания Cloudflare API токена
3. Введите API ключ и сохраните

### Шаг 2: Проверка и деплой

После настройки API ключа выполните:

```bash
# Перейти в директорию проекта
cd /home/user/webapp

# Вызвать setup_cloudflare_api_key для настройки окружения
# (после настройки в Deploy tab)

# Убедиться что проект собран
npm run build

# Создать проект на Cloudflare Pages (первый раз)
npx wrangler pages project create logistikpro \
  --production-branch main \
  --compatibility-date 2024-01-01

# Задеплоить проект
npm run deploy:prod
```

### Шаг 3: Получение Production URL

После успешного деплоя вы получите URLs:
- **Production**: `https://logistikpro.pages.dev`
- **Branch**: `https://main.logistikpro.pages.dev`

## 📦 Альтернативный вариант: Подключение к GitHub

Если вы хотите настроить автоматический деплой через GitHub:

### 1. Настройка GitHub

```bash
cd /home/user/webapp

# Вызвать setup_github_environment
# Если не настроен - перейти во вкладку #github для авторизации

# После успешной настройки
git remote add origin https://github.com/YOUR_USERNAME/logistikpro.git
git push -f origin main
```

### 2. Подключение в Cloudflare Dashboard

1. Зайдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в **Workers & Pages**
3. Нажмите **Create application** → **Pages** → **Connect to Git**
4. Выберите ваш репозиторий `logistikpro`
5. Настройте Build:
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
   - **Root directory**: `/`
6. Нажмите **Save and Deploy**

## 🔧 Команды для управления

```bash
# Локальная разработка
npm run build                 # Сборка проекта
pm2 start ecosystem.config.cjs # Запуск локального сервера
pm2 restart logistikpro       # Перезапуск
pm2 logs logistikpro --nostream # Просмотр логов

# Деплой
npm run deploy:prod           # Деплой на Cloudflare Pages

# Git
git add .
git commit -m "Update"
git push origin main

# Тестирование
curl -L http://localhost:3000/
curl http://localhost:3000/api/health
```

## 📝 Структура файлов для деплоя

```
dist/
├── _worker.js              # Cloudflare Worker (API endpoints)
├── _routes.json            # Маршрутизация (static files bypass worker)
└── static/                 # Статические файлы
    ├── index.html
    ├── calculator.html
    ├── request.html
    ├── contacts.html
    ├── css/
    ├── js/
    └── services/
```

## ⚠️ Важные замечания

1. **API ключ обязателен** - без него деплой невозможен
2. **cloudflare_project_name** - сохранён как `logistikpro`
3. **Статические файлы** - обслуживаются напрямую Cloudflare Pages (не через Worker)
4. **API endpoints** - обрабатываются Cloudflare Worker через Hono
5. **_routes.json** - настроен так, чтобы `/static/*` обходил Worker

## 🎯 Следующие шаги

После успешного деплоя:

1. **Обновить README.md** с production URL
2. **Настроить custom domain** (опционально)
3. **Добавить backend для форм** (интеграция с D1 или email API)
4. **Настроить мониторинг** через Cloudflare Analytics

---

**Проект готов к деплою! Следуйте инструкциям выше для публикации на Cloudflare Pages.**
