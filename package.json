{
  "name": "logistikpro",
  "version": "1.0.0",
  "description": "Сайт транспортной компании ЛогистикПро",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build && cp -r public/* dist/ && echo '{\"version\":1,\"include\":[\"/*\"],\"exclude\":[\"/static/*\"]}' > dist/_routes.json",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name logistikpro",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250705.0",
    "@hono/vite-dev-server": "^0.16.0",
    "vite": "^5.4.11",
    "wrangler": "^3.78.0",
    "typescript": "^5.6.3"
  }
}
